# Database
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Note: Copy this to .env and replace with your actual database URL
# For local development, you can use: "file:./dev.db" with provider = "sqlite"
# For production (Vercel), use PostgreSQL connection string from Neon or Vercel Postgres
